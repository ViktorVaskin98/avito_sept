# Разбор данных

Заполняю по ходу работы, сейчас тут план разбора и первые замеры из `notebooks/01_overview.ipynb`.

## Что уже посчитано

| Наблюдение | Число |
|---|---|
| Строк в train | 497 673 |
| Уникальных объявлений в train | 344 825 |
| Уникальных запросов в train (по полному набору признаков) | 354 463 |
| Уникальных текстов запроса в train | 74 529 |
| Объявлений в корпусе бенчмарка | 189 212 |
| Запросов в бенчмарке | 2 452 |
| Объявлений корпуса, встречающихся в train | 18 142 (9.6%) |
| Текстов запроса бенчмарка, встречающихся в train | 37.0% |
| Полных ключей запроса бенчмарка, встречающихся в train | 4.4% |
| Объявлений на уникальный запрос в train | среднее 1.40, медиана 1, ровно одно у 83.6% |
| Пар, где локация поиска равна локации объявления | 83.1% |
| Запросов бенчмарка, у которых в их локации нет ни одного объявления корпуса | 17.4% |
| Покрытие токенов `search_infm_params_text` параметрами объявления | среднее 0.971, полное у 92.0% |
| Пустой `search_infm_params_text` | 33% в train, 63% в бенчмарке |
| Уникальных `item_microcat_id` в корпусе | 752 |
| Объявлений в ячейке (локация × микрокатегория) | среднее 3.8, медиана 1 |

## Что ещё разобрать

- структура `item_infm_params_text`: словарь ключей, парсер, что из параметров реально полезно
- иерархия локаций: восстановление координат локации по `item_latitude` / `item_longitude`,
  расстояние вместо совпадения id
- лексический разрыв между запросом и заголовком: доля запросов, где пересечение токенов пустое
- распределение длин запросов и заголовков, доля запросов из одного слова
- цена, рейтинг, число отзывов: есть ли смещение в сторону выбранных объявлений
- чем запросы бенчмарка отличаются от запросов train по распределениям
