# Локальная валидация

```bash
uv run avito-cg split
```

Собирает копию бенчмарка из `train.parquet`, сверяет её с настоящим, проверяет на утечки,
гоняет две контрольные модели и сохраняет всё в `data/interim/local_benchmark/`.
Занимает около 70 секунд. Код в [`src/avito_cg/eval/benchmark.py`](../src/avito_cg/eval/benchmark.py).

## Зачем

Платформа даёт 7 попыток на 2 452 запроса. Подбирать по ним веса слияния и гиперпараметры нельзя:
попыток не хватит, и это прямое переобучение под тест. Значит нужна локальная копия, которая
предсказывает платформенный скор хотя бы по знаку изменений.

Наивный сплит «отложили запросы, взяли случайные объявления в корпус» врёт в двух местах,
и оба видны по цифрам из [EDA](EDA.md).

## Проблема первая: пересечение корпуса с обучением

В настоящем корпусе только **9.6%** объявлений встречаются в обучающих парах. Если набрать корпус
случайными объявлениями из train, знакомыми окажутся все 100%, и любой признак уровня объявления
(сколько раз его выбирали, его популярность) будет выглядеть сильнее, чем он есть на самом деле.
На реальном корпусе такого признака просто нет у девяти объявлений из десяти.

Проблема в том, что в train каждое объявление по определению хотя бы раз выбрано, то есть
незнакомых объявлений там нет вовсе. Их приходится создавать: часть запросов уходит
в **пул-донор**, их пары выбрасываются из обучения целиком, а их объявления идут в корпус
как незнакомые дистракторы.

Границу между обучающей частью и донором я не подбираю долями, а нахожу одним проходом.
Иду по перемешанным ключам запросов, переношу их в донор и держу для каждого объявления счётчик
оставшихся обучающих пар. Как только счётчик обнуляется, объявление становится незнакомым.
Останавливаюсь, когда незнакомых набралось столько, сколько нужно корпусу.

**Цена такого сплита.** Обучающих пар остаётся 213 449 из 497 673, это 42.9%. Остальные 280 337 пар
(199 753 запроса) уходят в донор. Это дорого, поэтому доля знакомых вынесена в параметр:

```bash
uv run avito-cg split --seen-share 1.0
```

вернёт наивный сплит со всеми парами в обучении. Так быстрее итерироваться, но абсолютные числа
будут завышены, и для финальных решений я пользуюсь значением по умолчанию.

Важная оговорка: не все компоненты решения чувствительны к этой доле. BM25, плотный поиск,
гео и классификатор микрокатегорий считают скор по содержимому объявления, и им всё равно,
встречалось ли оно в обучении. Чувствительны только признаки уровня объявления, и именно
из-за них сплит и устроен так.

## Проблема вторая: запросы бенчмарка другие

| | обучение | бенчмарк |
|---|---|---|
| слов в запросе, среднее | 2.45 | 3.20 |
| доля пустых фильтров | 35.2% | 63.1% |
| распределение по локациям | по всей стране | сконцентрировано в крупных городах |

KS-статистика по длине запроса 0.266. Если набрать отложенные запросы случайно, локальная
выборка получится короче, с фильтрами и размазанной по стране, а значит будет мерить не то.

Третью строку я в первой версии пропустил, и это дорого обошлось. Согласовал только длину
и наличие фильтра, получил на слиянии лексики с гео 0.892 и вместо того, чтобы записать
это в журнал, полез проверять переносимость. Проверка простая: сколько объявлений корпуса
лежит в радиусе 25 км от центра поисковой локации.

| | медиана | p25 |
|---|---|---|
| локальный корпус, первая версия | 1642 | 689 |
| настоящий корпус | 3261 | 1124 |

Вокруг локальных запросов оказалось вдвое просторнее. «Быть рядом» локально более редкое
свойство, чем на платформе, значит гео различает лучше, чем будет различать, и 0.892 завышено.
Выяснилось бы это только на платформе, где попыток семь.

**Как чиню.** Вместо стратификации по двум переменным - **раскинг**, он же iterative
proportional fitting. Итеративно подгоняю веса запросов так, чтобы выборка совпала с бенчмарком
сразу по трём маргиналам: локация, наличие фильтра, число слов. Совместное распределение
по 273 локациям на 2452 запросах не набрать, а маргиналы согласовать можно. Локации, которых
в бенчмарке нет вовсе, получают нулевой вес: если по такой локации там не ищут,
то и в локальной выборке ей делать нечего.

Прямая мера того, сработало ли это, лежит теперь в самой сверке, строкой «объявлений
в радиусе 25 км»: 3274 против 3261, расхождение меньше половины процента.

Разметки в запросах бенчмарка нет, распределение признаков доступно легально, так что
это обычное перевзвешивание по наблюдаемым ковариатам, а не подглядывание в ответы.

## Что получилось

| величина | локальный | настоящий |
|---|---|---|
| запросов | 2 452 | 2 452 |
| объявлений в корпусе | 189 212 | 189 212 |
| слов в запросе, среднее | 3.196 | 3.197 |
| доля пустых фильтров | 0.634 | 0.631 |
| доля корпуса, знакомая обучению | 0.096 | 0.096 |
| объявлений в радиусе 25 км, медиана | 3 274 | 3 261 |
| локаций в корпусе | 2 318 | 2 877 |
| микрокатегорий в корпусе | 205 | 752 |
| релевантных на запрос, среднее | 1.261 | неизвестно |
| релевантных на запрос, ровно один | 0.866 | неизвестно |

Обучающих пар остаётся 213 449 из 497 673. Ещё одно число из meta: **28.7%** отложенных
релевантных объявлений всё-таки встречаются в обучающей части через другие запросы.
Это выше корпусных 9.6%, и так и должно быть: популярное объявление и выбирают чаще,
и в обучение оно попадает вероятнее. На настоящем бенчмарке эта величина не измерима.

## Контрольные модели

Сплит без проверки самого сплита ничего не стоит, поэтому вместе с ним гоняются две модели,
которые нужны не ради качества, а ради того, чтобы убедиться в сходимости всей цепочки
«разметка - предсказание - метрика».

| модель | Recall@50 | 95% ДИ |
|---|---|---|
| случайные 50 | 0.00041 | [0.0000, 0.0012] |
| 50 ближайших по гео | 0.12209 | [0.1094, 0.1350] |

Случайный отбор должен давать примерно 50/189212 = 0.00026, и наблюдаемое значение накрывается
интервалом. Если бы там оказалось 0.01, значит в корпусе слишком мало дистракторов или в разметку
затекли лишние объявления.

Вторая строка интереснее: одна география, вообще без текста, даёт **0.122**. Это отправная точка,
с которой надо сравнивать всё дальнейшее.

## Проверка на утечки

`leakage_checks` возвращает список претензий, и пустой список это условие для того, чтобы
пользоваться сплитом. Проверяется:

- ни один отложенный запрос не попал в обучающие пары
- все релевантные объявления лежат в корпусе
- в корпусе нет дублей
- нет запросов без релевантных
- список запросов и разметка не разошлись
- достигнутая доля знакомых объявлений совпадает с заданной

То же самое покрыто тестами в [`tests/test_benchmark.py`](../tests/test_benchmark.py)
на синтетических данных, включая проверку, что перевзвешивание страт действительно
двигает выборку к целевому распределению, а не просто меняет seed.

## Третья часть сплита: донорский пул как обучающая выборка

Пул-донор задумывался как источник незнакомых объявлений для корпуса. Оказалось, что он же
решает другую задачу, про которую я вначале не думал.

Переранжировщику нужны размеченные запросы, а отложенные для этого не годятся: на них же
считается метрика, и обучаться на них можно только с кросс-валидацией, то есть на трёх тысячах
позитивов. А донорские запросы не видел ни один сигнал: их пары не участвуют ни в калибровке
гео, ни в фасетах, ни в обучении энкодера. При этом их объявления как раз и лежат в корпусе.
Значит для них есть и признаки, и метки, и никакой утечки. Таких запросов 161 456.

Влобовую брать их нельзя, и это важная деталь. Доля знакомых обучению среди позитивов
донорских запросов 4.2%, среди позитивов отложенных 28.7%: донор по построению состоит
из объявлений, которые в разрешённую часть не попали. Модель выучила бы, что популярное
объявление скорее лишнее, то есть ровно обратное тому, что происходит на отложенных запросах.

Поэтому донорские запросы набираются так, чтобы доля знакомых среди их позитивов совпала
с отложенными, тем же приёмом согласования маргиналов, что и сам сплит. Получилось 16 826
запросов, 27 553 позитива, доля знакомых 0.28872 против цели 0.28872.

Собирается это командой `avito-cg donor-set`, код в [`train/extra.py`](../src/avito_cg/train/extra.py),
тесты в [`tests/test_extra.py`](../tests/test_extra.py).

## Чего эта конструкция не умеет

Честный список, потому что расхождение с платформенным скором ожидаемо и его причины надо знать
заранее.

1. **Корпус состоит из выбранных объявлений.** В train по определению попали только те, что
   кто-то однажды выбрал. Настоящий корпус это полный срез, включая объявления, которые
   не выбирал никто. Наши дистракторы «качественнее» настоящих, то есть задача локально
   может оказаться чуть сложнее по ранжированию и чуть легче по полноте.
2. **Микрокатегорий 205 вместо 752.** В train их всего 212, взять остальные неоткуда.
   Правда, по EDA 99.1% настоящего корпуса лежит в знакомых микрокатегориях,
   так что расхождение больше по разнообразию, чем по массе.
3. **Число релевантных на запрос неизвестно.** Локально получилось 1.261 против маргинальных
   1.40 по train, и это следствие перевзвешивания. Что там на самом деле, без разметки не узнать.
4. **Один сплит это одна реализация.** Для важных решений стоит прогонять два-три seed
   и смотреть, переживает ли улучшение смену сплита.
5. **Согласованы маргиналы, а не совместное распределение.** Раскинг выравнивает каждый
   признак по отдельности. Если на платформе, скажем, длинные запросы систематически идут
   из мелких городов, а локально наоборот, маргиналы сойдутся, а совместное нет.
   Проверять такое приходится точечно, как я проверил гео-плотность.
6. **Разрешающая способность около 0.01, и это жёсткий предел.** 2 452 запроса с метрикой,
   которая ведёт себя как hit-rate, не различают приросты в тысячные доли - ни в плюс,
   ни в минус. Пятая отправка это показала прямо: шаг, локально неотличимый от нуля,
   на платформе дал +0.0104. Вывод не в том, что локальной оценке нельзя верить, а в том,
   что у неё есть порог, ниже которого она молчит, и молчание нельзя читать как отказ.

## Как этим пользоваться

```python
from avito_cg.config import PATHS
from avito_cg.data.io import load_train
from avito_cg.eval.benchmark import LocalBenchmark
from avito_cg.eval.metrics import bootstrap_ci, recall_at_k, recall_per_query

train = load_train()
local = LocalBenchmark.load(PATHS.interim / "local_benchmark")

corpus = local.corpus(train)          # 189 212 объявлений, среди которых ищем
pairs = local.fit_pairs(train)        # только то, чем разрешено обучаться

predictions = my_model(local.queries, corpus)
print(recall_at_k(local.relevant, predictions))
print(bootstrap_ci(recall_per_query(local.relevant, predictions)))
```

Доверительный интервал тут не украшение. У 86.6% отложенных запросов ровно один релевантный
ответ, метрика ведёт себя как hit-rate, и её стандартная ошибка на 2 452 запросах около 0.01.
Улучшение меньше этой величины локально неотличимо от шума - и «неотличимо» здесь значит
именно это, а не «отсутствует»: второй раунд признаков переранжировщика дал локально
+0.0016 при интервале, накрывающем ноль, а на платформе +0.0104.
