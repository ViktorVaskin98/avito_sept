# Кандидатогенерация для поиска услуг на Авито

Задача: по короткому поисковому запросу отобрать до 50 объявлений-кандидатов из корпуса
на 189 212 объявлений услуг. Метрика Recall@50, усреднённая по 2 452 запросам бенчмарка.
Полное условие лежит в [docs/TASK.md](docs/TASK.md).

Всё считается локально на ноутбуке без дискретной видеокарты (Ryzen 5 7640HS, 16 ГБ RAM),
обращений к внешним API нет.

## Результат

| Конфигурация | Recall@50 локально | Recall@50 на платформе |
|---|---|---|
| в работе | | |

Журнал экспериментов: [docs/EXPERIMENTS.md](docs/EXPERIMENTS.md)

## Что я нашёл в данных

Разбор с цифрами и графиками в [docs/EDA.md](docs/EDA.md), здесь только то, что определило решение.

**Корпус почти не пересекается с обучением.** Из 189 212 объявлений бенчмарка в `train.parquet`
встречаются только 18 142, это 9.6%. Запоминать пары «запрос - объявление» бесполезно,
ядро решения обязано быть контентным.

**На запрос приходится одно релевантное объявление.** В train на уникальный запрос в среднем
1.40 объявления, у 83.6% ровно одно. Recall@50 фактически ведёт себя как hit-rate, и разброс
метрики на 2.5 тысячах запросов порядка 0.01, так что гоняться за приростом в 0.005 бессмысленно.

**`search_infm_params_text` это фасетный фильтр, а не просто текст.** Взял 333 674 пары
с непустым фильтром: токены фильтра покрываются параметрами выбранного объявления в среднем
на 0.971, полностью у 92.0% пар. Дословной подстрокой только у 53.8%, потому что формулировки
ключей расходятся (в запросе «Тип услуги автосервиса», в объявлении «Тип услуги»).

**Локация решает, но `location_id` иерархический.** В 83.1% пар локация поиска совпадает
с локацией объявления. При этом у 17.4% запросов бенчмарка в их локации нет вообще ни одного
объявления корпуса: запрос пришёл с родительского узла, а объявления лежат в дочерних.
Плоское сравнение id тут ломается, нужна геометрия по координатам.

**Категории бесполезны.** `search_category` и `item_category_id` равны 114 почти везде,
а `search_is_delivery_search` в бенчмарке равен нулю у всех 2 452 запросов. Реальная таксономия
одна, это `item_microcat_id` на 752 значения. Ячейка (локация × микрокатегория) содержит
в медиане 1 объявление, то есть связка гео плюс микрокатегория сужает поиск до десятка вариантов.

## Подход

Гибридная кандидатогенерация с переранжированием:

1. Лексика: BM25 по заголовку, фасетам и голове описания, плюс отдельный индекс по символьным
   n-граммам против опечаток и словоформ
2. Семантика: би-энкодер на базе `rubert-tiny2`, дообученный на train-парах с in-batch negatives
3. Приоры: классификатор `текст запроса -> микрокатегория` и гео-приор по восстановленным
   координатам локаций
4. Слияние скоров с весами, подобранными на локальной валидации
5. Переранжирование кандидатов градиентным бустингом на hard negatives, топ-50 в ответ

Подробно с обоснованием каждого шага: [docs/SOLUTION.md](docs/SOLUTION.md)

## Как я проверяю качество

Платформа даёт 7 попыток, поэтому основная проверка локальная. Из `train.parquet` собирается
копия бенчмарка: 2 452 отложенных запроса, корпус на 189 212 объявлений, доля «знакомых»
объявлений подогнана под реальные 9.6%, доля пустых фильтров под реальные 63%. Все строки
отложенных запросов исключаются из всего, что обучается. Детали и проверки на утечки:
[docs/VALIDATION.md](docs/VALIDATION.md)

## Запуск

```bash
uv venv --python 3.12
uv pip install -e ".[dev]"
```

Данные в репозиторий не входят. Скачай архив по ссылке из [docs/TASK.md](docs/TASK.md)
и распакуй три parquet-файла в `data/raw/`, дальше:

```bash
uv run avito-cg check-data
```

Полный путь от сырых данных до ответа:

```bash
uv run avito-cg build-index
uv run avito-cg train-model
uv run avito-cg evaluate
uv run avito-cg make-answer --out submissions/answer.csv
uv run avito-cg validate-answer submissions/answer.csv
```

Тесты:

```bash
uv run pytest
```

## Структура

```
docs/         условие, разбор данных, описание решения, журнал экспериментов
notebooks/    EDA и разбор ошибок, вся логика импортируется из пакета а не дублируется
src/avito_cg/
  data/       чтение parquet, нормализация текста, парсер параметров объявления
  index/      лексический индекс, плотный индекс, гео, приоры
  retrieval/  генерация кандидатов и слияние скоров
  train/      дообучение би-энкодера и обучение переранжировщика
  eval/       метрика, сборка локального бенчмарка, проверка формата ответа
  cli/        точки входа
tests/        метрика и формат ответа
data/         не в репозитории: raw / interim / artifacts
```

## Использованные open-source компоненты

| Компонент | Зачем |
|---|---|
| `pyarrow`, `pandas`, `numpy`, `scipy` | чтение parquet, разреженные матрицы |
| `scikit-learn` | токенизация, каркас под BM25, классификатор микрокатегорий |
| `PyStemmer` | стеммер Snowball для русского |
| `torch`, `transformers` | би-энкодер |
| `cointegrated/rubert-tiny2` | предобученные веса энкодера, дообучаю локально |
| `catboost` | переранжирование кандидатов |

Все модели разворачиваются локально, обращений к внешним API нет.
